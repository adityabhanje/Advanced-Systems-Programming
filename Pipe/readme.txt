1) The input file through which the mapper reads it's input is named as"input.txt"
2) For executing the mapper, reducer and combiner, just type "make" in the command prompt (makefile is used to compile all the codes)
